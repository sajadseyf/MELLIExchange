{
  "name": "melli-exchange",
  "version": "0.1.0",
  "private": true,
  
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "bash -c 'ulimit -n 65536 && pnpm -r --parallel --stream dev'",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "seed": "pnpm --filter @melli/api seed"
  },
  "engines": {
    "node": ">=20"
  }
}
