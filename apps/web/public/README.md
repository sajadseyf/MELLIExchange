# Public assets

Place the brand logo at `logo.png` (the LogoMark component loads `/logo.png`).

```
apps/web/public/logo.png
apps/admin/public/logo.png
```

Same file in both app `public/` folders for now.
