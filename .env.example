# Copy to .env and fill values. Each app also has its own .env.example.

# --- API ---
PORT=4000
#MONGO_URI=mongodb://localhost:27017/melli_exchange
MONGO_URI=mongodb+srv://sajad_seyf:<db_password>@cluster0.033u1m7.mongodb.net/?appName=Cluster0
JWT_SECRET=change-me-to-a-long-random-string
COOKIE_DOMAIN=localhost
NODE_ENV=development

# Admin seed (used once by `pnpm seed`)
ADMIN_EMAIL=admin@melliexchange.local
ADMIN_PASSWORD=changeme

# --- Admin / Web ---
NEXT_PUBLIC_API_URL=http://localhost:4000
